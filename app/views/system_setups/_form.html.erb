<%= Beekeeper.current %>
<%= form_for(@system_setup) do |f| %>
  <% if @system_setup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@system_setup.errors.count, "error") %> prohibited this system_setup from being saved:</h2>

      <ul>
      <% @system_setup.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
