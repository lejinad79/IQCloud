<div class="row">
  <div class="col-md-2"></div>
  <div class="col-md-8">
    <h3>Welcome <%= current_beekeeper.first_name %> <%= current_beekeeper.last_name %> !</h3>
    <p>Let's get started!!!</p>
    <%= form_for(@apiary , :url => wizard_path, :method => :put) do |f| %>
      <% if @apiary.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@organization.errors.count, "error") %> prohibited this organization from being saved:</h2>

          <ul>
          <% @apiary.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <div class="form-group">
        <%= f.text_field :name, :class=>"form-control", :placeholder => "Apiary Name"  %>
      </div>
      <div class="form-group">
        <%= f.text_area :description, :class=>"form-control", :rows=>"3", :placeholder => "Description" %>
      </div>

      <%= f.submit 'Create', :class=>"btn btn-default" %>
    <% end %>
  </div>
  <div class="col-md-2"></div>
</div>
