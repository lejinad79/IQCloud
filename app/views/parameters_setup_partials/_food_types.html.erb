<%= f.fields_for :food_types do |v| %>
    <table class="table table-striped">
      <tr>
        <td><%= v.text_field :name, :class => 'form-control', :placeholder => 'Food type' %></td>
        <td><%= v.link_to_remove "Remove", :class => 'btn btn-danger' %></td>
      </tr>
    </table>
<% end %>
<p><%= f.link_to_add "Add", :food_types, :class => 'btn btn-primary' %></p>