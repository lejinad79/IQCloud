<%= form_for(@apiary) do |f| %>
  <% if @apiary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@apiary.errors.count, "error") %> prohibited this apiary from being saved:</h2>

      <ul>
      <% @apiary.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :apiary_type_id %><br>
    <%= f.select :apiary_type_id, ApiaryType.owner(current_beekeeper.current_apiary_id).all.map { |i| [ i.name, i.id ] } %>
  </div>
  <div class="field">
    <%= f.label :apiary_forage_type_id %><br>
    <%= f.text_field :apiary_forage_type_id %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
